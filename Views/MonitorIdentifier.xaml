<Window
    x:Class="MonitorControl.MonitorIdentifier"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MonitorControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >
    <Viewbox VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="5">
        <StackPanel Orientation="Horizontal" x:Name="box">
            <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE7F4;" Margin="5"/>
            <TextBlock Text="{x:Bind Label, Mode=OneTime}" FontWeight="SemiBold" Margin="5"/>
        </StackPanel>
    </Viewbox>
</Window>
