<Window
    x:Class="MonitorControl.PopupMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MonitorControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ItemsRepeater x:Name="ItemList">
        <ItemsRepeater.Layout>
            <StackLayout/>
        </ItemsRepeater.Layout>
        <ItemsRepeater.ItemTemplate>
            <local:PopupMenuDataTemplateSelector>
                <local:PopupMenuDataTemplateSelector.Item>
                    <DataTemplate x:DataType="local:PopupMenuItem">
                        <ToggleMenuFlyoutItem Text="{x:Bind Text}" Click="{x:Bind Clicked}" IsChecked="{x:Bind IsChecked, Mode=OneWay}"/>
                    </DataTemplate>
                </local:PopupMenuDataTemplateSelector.Item>
                <local:PopupMenuDataTemplateSelector.Seperator>
                    <DataTemplate>
                        <MenuFlyoutSeparator/>
                    </DataTemplate>
                </local:PopupMenuDataTemplateSelector.Seperator>
            </local:PopupMenuDataTemplateSelector>
        </ItemsRepeater.ItemTemplate>
        
    </ItemsRepeater>
</Window>
