<Window 
    x:Class="MonitorControl.MainWindow" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:local="using:MonitorControl" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d"
    >
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.Resources>
            <DataTemplate x:DataType="local:Monitor" x:Key="MonitorItemTemplate">
                <local:MonitorTweaker DataContext="{Binding}"/>
            </DataTemplate>
            <DataTemplate x:DataType="x:String" x:Key="ProfileItemTemplate">
                <local:Profile DataContext="{Binding}"/>
            </DataTemplate>
            <local:MonitorControlDataTemplateSelector x:Key="TemplateSelector"
                Monitor="{StaticResource MonitorItemTemplate}"
                Profile="{StaticResource ProfileItemTemplate}"
            />
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="44" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border x:Name="AppTitleBar"
                Grid.Column="1"
                IsHitTestVisible="True"
                VerticalAlignment="Top"
                Height="44">
            <TextBlock x:Name="AppTitle"
                       Text="Monitor Control"
                       VerticalAlignment="Center"
                       Margin="18,0,0,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource CaptionTextBlockStyle}">
                        <Setter Property="FontSize" Value="15"/>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Border>
        <NavigationView x:Name="NavLinksList" Grid.Row="1" IsBackButtonVisible="Collapsed" IsSettingsVisible="False"
            MenuItemsSource="{x:Bind Instance.Monitors}">
            <NavigationView.DataContext>
                <local:MonitorFn x:Name="Instance"></local:MonitorFn>
            </NavigationView.DataContext>
            <NavigationView.MenuItemTemplate>
                <DataTemplate x:DataType="local:Monitor">
                    <NavigationViewItem Icon="GoToStart" Content="{x:Bind Description}" Tag="{x:Bind Description}" />
                </DataTemplate>
            </NavigationView.MenuItemTemplate>
            <NavigationView.PaneFooter>
                <StackPanel Orientation="Vertical" Visibility="Collapsed">
                </StackPanel>
            </NavigationView.PaneFooter>
            <Frame 
                ContentTemplateSelector="{StaticResource TemplateSelector}" 
                DataContext="{Binding ElementName=NavLinksList, Path=SelectedItem}"/>
        </NavigationView>
    </Grid>
</Window>